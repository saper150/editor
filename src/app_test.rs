// use crate::glfw::WindowEvent;
// use crate::program::Program;

#[test]
fn should_launch() {
    // let mut program = Program::new();

    // {
    //     let point = program.app.text.current_point();
    //     point.text.remove(0..point.text.len_chars());
    //     assert_eq!(point.text.to_string(), "");
    // }

    // {
    //     Program::process_event(&mut program.app, &glfw::WindowEvent::Char('a'));
    //     let point = program.app.text.current_point();
    //     assert_eq!(point.text.to_string(), "a");
    // }
}
